# LEARNING-[Issue/Topic]

**Date:** [YYYY-MM-DD]
**Author:** [Agent Name or Human]
**Context:** [Briefly describe the task or issue that led to this learning]

## The Problem
What was the challenge? (e.g., A performance bottleneck in `ask patches`, UI widget overlapping, emergent behavior failing to stabilize).

## The Solution
What was the architectural, workflow, or code-level solution? Provide a clear explanation and code snippets if applicable.

## Reusable Takeaway
How should future agents or humans apply this knowledge? (e.g., "Always use `with` instead of `if` inside `ask` blocks for performance.")
